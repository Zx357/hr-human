# ========================================
# KAdmin 生产环境配置
# 所有敏感信息通过环境变量注入
# ========================================

server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/kadmin?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: kadmin
    password: DiFdxbpZT2pEYyKx
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password:

# 生产环境日志级别
logging:
  level:
    root: warn
    com.kadmin: info
    com.kadmin.mapper: warn

# 生产环境关闭 Swagger
knife4j:
  enable: false

springdoc:
  api-docs:
    enabled: false
