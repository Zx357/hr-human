<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kadmin.mapper.HrRewardPunishMapper">
    <select id="selectPageWithEmployee" resultType="com.kadmin.entity.HrRewardPunish">
        SELECT rp.*, e.name as employee_name, e.employee_no, d.unit_name as dept_name,
               CASE 
                   WHEN d.unit_type = 2 THEN d.unit_name
                   WHEN p1.unit_type = 2 THEN p1.unit_name
                   WHEN p2.unit_type = 2 THEN p2.unit_name
                   WHEN p3.unit_type = 2 THEN p3.unit_name
                   ELSE NULL
               END as company_name
        FROM hr_reward_punish rp
        LEFT JOIN hr_employee e ON rp.employee_id = e.id
        LEFT JOIN org_unit d ON e.dept_id = d.id AND d.deleted = 0
        LEFT JOIN org_unit p1 ON d.parent_id = p1.id AND p1.deleted = 0
        LEFT JOIN org_unit p2 ON p1.parent_id = p2.id AND p2.deleted = 0
        LEFT JOIN org_unit p3 ON p2.parent_id = p3.id AND p3.deleted = 0
        WHERE rp.deleted = 0
        <if test="employeeName != null and employeeName != ''">
            AND e.name LIKE CONCAT('%', #{employeeName}, '%')
        </if>
        <if test="type != null">
            AND rp.type = #{type}
        </if>
        <if test="status != null">
            AND rp.status = #{status}
        </if>
        ORDER BY rp.created_time DESC
    </select>
</mapper>
