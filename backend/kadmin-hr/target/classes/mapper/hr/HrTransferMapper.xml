<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kadmin.hr.mapper.HrTransferMapper">
    <select id="selectPageWithEmployee" resultType="com.kadmin.hr.domain.HrTransfer">
        SELECT t.*, e.name as employee_name, e.employee_no,
               fc.unit_name as from_company_name, tc.unit_name as to_company_name,
               fd.unit_name as from_dept_name, td.unit_name as to_dept_name
        FROM hr_transfer t
        LEFT JOIN hr_employee e ON t.employee_id = e.id
        LEFT JOIN org_unit fc ON t.from_company_id = fc.id
        LEFT JOIN org_unit tc ON t.to_company_id = tc.id
        LEFT JOIN org_unit fd ON t.from_dept_id = fd.id
        LEFT JOIN org_unit td ON t.to_dept_id = td.id
        WHERE 1 = 1
        <if test="employeeName != null and employeeName != ''">
            AND e.name LIKE CONCAT('%', #{employeeName}, '%')
        </if>
        <if test="transferType != null and transferType != ''">
            AND t.transfer_type = #{transferType}
        </if>
        <if test="status != null">
            AND t.status = #{status}
        </if>
        ORDER BY t.created_time DESC
    </select>
</mapper>
